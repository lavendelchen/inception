server {
	server_name shaas.42.fr;

	listen 443 ssl;
	ssl_certificate		/etc/nginx/ssl/shaas.crt;
	ssl_certificate_key	/etc/nginx/ssl/shaas.key;
	ssl_protocols		TLSv1.2 TLSv1.3;

	root /var/www/html/; #?
	index light_index.html; #?

	# Directive necessaire pour les endpoints, 
	# Fast cgi necessaire pour nginx pour "traduire" le php
	#location / {
	#	try_files $uri $uri/ /index.php$is_args$args;
	#}
#
	#location ~ \.php$ {
	#	fastcgi_split_path_info ^(.+\.php)(/.+)$;
	#	fastcgi_pass wordpress:9000; #PHP for wordpress will listen on the port 9000
	#	fastcgi_index index.php;
	#	include fastcgi_params;
	#	fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
	#	fastcgi_param SCRIPT_NAME $fastcgi_script_name;
	#}
}